mixin comment(info)
    li.comment__wrap
        img(src=info.owner.avatar).comment__img
        div.comment__text
            a(href=`/users/${info.owner._id}`) #{info.owner.email}
            p=info.text