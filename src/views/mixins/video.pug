mixin video(info)
    div
        h4
            a(href=`/videos/${info.id}`)=info.title
        p=info.description
        ul
            each hastag in info.hashTags
                li=hastag
        small=info.createAt
        hr